# Neural Control Barrier Functions (NCBF) Requirements
# Generated from latentSafety conda environment
# Python 3.12 compatible
# generated by claude

# Core Scientific Computing
numpy==1.26.4
scipy==1.16.2
matplotlib==3.10.6
seaborn==0.13.2
pandas==2.3.2
h5py==3.14.0

# PyTorch for Neural Networks
torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0

# Computer Vision and Image Processing
pillow==11.3.0
imageio==2.37.0
imageio-ffmpeg==0.6.0
moviepy==1.0.3

# Machine Learning and Optimization
tqdm==4.67.1
tensorboard==2.20.0
tensorboard-data-server==0.7.2

# Control and Robotics
gym==0.26.2
gymnasium==0.28.1

# Mathematical Computing
sympy==1.14.0
numba==0.62.0
networkx==3.5

# Data Visualization and Plotting
matplotlib-inline==0.1.7

# CLI and Progress
termcolor==3.1.0

# Configuration and Utilities
pyyaml==0.17.40
ruamel-yaml==0.17.40

# Optional: For advanced control (if needed)
# clarabel==0.11.1  # QP solver (currently using analytical solution)
# cvxpy==1.4.1  # Convex optimization (optional, for comparison)

# Development Tools (for debugging and analysis)
# jupyter related packages are available but optional for production
# ipython==9.5.0
# ipykernel==6.30.1

# Note: CUDA packages are installed via conda:
# nvidia-cuda-cu12, nvidia-cudnn-cu12, etc.
# These should be installed via conda for proper GPU support

# System dependencies (install via conda):
# - mkl (Intel Math Kernel Library)
# - hdf5 (for HDF5 file support)
# - ffmpeg (for video processing)

# Installation Instructions:
# 1. Create conda environment with Python 3.12
# 2. Install PyTorch with CUDA support via conda
# 3. Install this requirements.txt via pip
# 4. Additional system packages may be needed for full functionality

# For GPU support, install PyTorch via conda:
# conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia

# For complete environment setup:
# conda create --name neural_clbf python=3.12
# conda activate neural_clbf
# conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia
# pip install -r requirements.txt
# pip install -e .  # Install the package in development mode